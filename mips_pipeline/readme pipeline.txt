arquivo contendo o codigo fonte sem o NCURSES
PIPELINE_ZERO.RAR

o arquivo contem um makefile onde o usuario
deve rodar o comando make no terminal
que o simulador já sera executado


contem também o codigo fonte do simulador
com o NCURSES de interface grafica
PIPELINE_NCURSES.RAR

o arquivo também contem um makefile para
o usuario executar o programa, porém 
nesse o usuario deve dar o comando make
no terminal e depois executar o simulador
manualmente com o seguinte comando "./exec"